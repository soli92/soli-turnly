---
type: gaps
project: soli-turnly
description: "Registro append-only dei gap di conoscenza (PATTERN R.5)"
---

# wiki/gaps.md — Gap di conoscenza

> **Append-only** — non modificare le entry esistenti (PATTERN R.5).
> Aggiornato dal wiki-keeper durante l'ingest.

| ID | Data | Tema | Descrizione | Status |
|---|---|---|---|---|
| G-001 | 2026-07-13 | Tech stack | `raw/tech_stack.md` assente: il documento funzionale non prescrive lo stack tecnologico. Necessario per Arch (design_&_architecture) e per i dev-agent. Suggerito: eseguire `/tech-scout` o aggiungere manualmente `raw/tech_stack.md`. | aperto |
| G-002 | 2026-07-13 | Design Figma | Inventario schermate §10 è funzionale (22 schermate descritte), ma nessun `raw/*.kb.json` Figma è ancora disponibile. Necessario per Visual Oracle, UX/UI Review, Prototype Generation. Suggerito: eseguire `/sync-docs` con fonte Figma quando i mockup saranno disponibili. | aperto |
| G-003 | 2026-07-13 | DB Schema — tabelle mancanti rispetto ad ADR-001 | ADR-001 definisce 12 tabelle: le 10 implementate in TSK-002 più `availability` e `coverage_requirements` (e `swap_operations` come tabella opzionale). TSK-002 scope era 10 tabelle; le 2 tabelle rimanenti (`availability`, `coverage_requirements`) vanno pianificate in un TSK separato (wave 2+) per non introduire drift rispetto ad ADR-001 §Schema. | chiuso — TSK-013 (2026-07-14): migration 0003_availability_coverage_swap.sql aggiunge `availability`, `coverage_requirements`, `swap_operations`; schema ora allineato ad ADR-001 §Schema (13 tabelle totali) |
| G-004 | 2026-07-14 | RF-B — campo `contractType` mancante da schema DB e API | TSK-016 richiede il campo `contractType` (enum: full-time/part-time/contractor) nell'anagrafica dipendenti. Il campo non esiste nella tabella `users` (solo `contract_hours`). `types/index.ts` definisce `ContractType` ma non è persisto in DB. La colonna "contratto" in StaffTable mostra un valore derivato (Full-time ≥36h, Part-time <36h) come workaround temporaneo. Necessaria migration BE + aggiornamento `adminUserCreateSchema` / API Route Handler. | aperto |
| G-005 | 2026-07-14 | RF-B — campo `telefono` mancante da schema DB e API | TSK-016 richiede il campo `telefono` (opzionale) nell'anagrafica dipendenti. Il campo non esiste nella tabella `users` né nei Zod schemas. Il form StaffModal non include questo campo. Necessaria migration BE + aggiornamento schema Zod + API Route Handler. | aperto |
| G-006 | 2026-07-14 | GET /api/admin/users — join qualifications mancante | Il Route Handler `GET /api/admin/users` non esegue LEFT JOIN con `qualifications`, quindi `qualificationName` non è incluso nella response. Il workaround attuale (TSK-016): il RSC `staff/page.tsx` fetcha direttamente da DB con join e passa i dati come `initialData` a TanStack Query. Al successivo refetch client-side, `qualificationName` sarà null per le righe aggiornate → la `StaffTable` usa il lookup client-side dalla prop `qualifications`. Soluzione definitiva: aggiungere il join al Route Handler. | aperto |
| G-007 | 2026-07-14 | API BE mancante — GET /api/admin/reports/overtime | TSK-014 (FE dashboard) richiede `GET /api/admin/reports/overtime?period=current_week` per il KPI ore straordinario. L'endpoint non esiste: non c'è route sotto `app/api/admin/reports/`. Il FE `OvertimeSummaryCard` gestisce 404/501 con graceful degradation (mostra "N/D"). Necessario: pianificare un TSK BE che implementa l'endpoint e la logica aggregativa delle ore straordinario dal motore regole (`lib/rules/calculateOvertime.ts`). | chiuso — TSK-027 (2026-07-14): route `app/api/admin/reports/overtime/route.ts` implementata con calcolo settimanale ISO, join shifts+users+qualifications, ordinamento per overtimeHours desc, soglia RB-06. Pagina FE `/admin/reports/overtime` con OvertimeReportClient + OvertimeFilters + OvertimeTable (TanStack Table v8) + OvertimeRowBadge + export CSV lato browser. |
| G-008 | 2026-07-14 | API BE — param `hasViolations` non implementato su GET /api/shifts | TSK-014 richiede `GET /api/shifts?hasViolations=true&from=today` per il KPI violazioni. La route `app/api/shifts/route.ts` non supporta il parametro `hasViolations`: lo ignora silenziosamente, restituendo tutti i turni (non filtrati per violazione). Il FE `ViolationSummary` mostra comunque il `total` ricevuto. Necessario: implementare il filtro `hasViolations` nel BE quando TSK-006 (motore regole) sarà completato e i dati di violazione saranno persistiti. | aperto |
| GAP-RECURRENCE-API-001 | 2026-07-14 | API BE mancante — POST /api/admin/recurrence/preview e /generate | TSK-019 (FE wizard ricorrenze) richiede due endpoint BE non ancora implementati: (1) `POST /api/admin/recurrence/preview` — dry-run: calcola i turni candidati e i conflitti (assenza/holiday/overlap) per un payload di tipo `RecurrenceWizardPayload`, risponde `{ turni: ShiftPreview[], conflicts: RecurrenceConflict[] }`; (2) `POST /api/admin/recurrence/generate` — genera effettivamente i turni per più utenti/ciclo rotativo ed emette evento Inngest `shift.recurrence.trigger`, risponde `{ generated: number, skipped: number, jobId?: string, status: pending\|done }`. Gli endpoint esistenti (`POST /api/admin/recurrences` – singolo utente/tipo turno) coprono solo la casistica semplice. Il FE è implementato e pronto; il codice chiama gli endpoint con i tipi definiti in `hooks/useRecurrences.ts`. Necessario: pianificare un TSK BE che implementa i due route handlers in `app/api/admin/recurrence/preview/route.ts` e `app/api/admin/recurrence/generate/route.ts`. | aperto |
| G-009 | 2026-07-14 | API BE mancante — GET /api/requests/:id/impact | TSK-020 (FE coda richieste) richiede `GET /api/requests/:id/impact` per il pannello anteprima impatto pre-approvazione (`ApprovalImpactPanel`). L'endpoint non esiste: nessuna route sotto `app/api/requests/[id]/impact/`. Il FE gestisce 404/501 con graceful degradation (pannello nascosto, il BE eseguirà comunque la rivalidazione RB-14 al POST /approve). Necessario: pianificare un TSK BE che implementa l'endpoint calcolando assenze bloccate, turni in conflitto (RB-01..08), e compatibilità scambi (RB-10) per il dipendente richiedente. | aperto |
| GAP-TSK022-001 | 2026-07-14 | API BE mancante — POST /api/requests/:id/reject-swap | TSK-022 (FE Le mie richieste) richiede `POST /api/requests/:id/reject-swap` per consentire al collega destinatario di rifiutare una proposta di scambio. L'endpoint non esiste: nessuna route sotto `app/api/requests/[id]/reject-swap/`. Il FE (`SwapAcceptRejectPanel`, `useRejectSwap`) gestisce 404/501 con graceful degradation (errore visibile). Necessario: pianificare un TSK BE che implementa l'endpoint con verifica T-SEC-08 (`payload.targetUserId === session.user.id`), transizione di stato da `awaiting_colleague` a `rejected`, notifica SSE al richiedente originale. | aperto |
| GAP-TSK022-002 | 2026-07-14 | API BE mancante — GET /api/requests?received_swap=true | TSK-022 (FE Le mie richieste) richiede `GET /api/requests?received_swap=true` per elencare le proposte di scambio turno ricevute da un collega (dove `payload->>'targetUserId' = session.user.id`). L'endpoint attuale `GET /api/requests` filtra solo `WHERE user_id = session.user.id`, quindi le proposte ricevute da altri dipendenti non sono visibili al collega destinatario. Il FE mostra un placeholder informativo con il codice gap. Necessario: aggiungere il parametro `received_swap=true` al Route Handler con query `WHERE payload->>'targetUserId' = session.user.id AND type = 'shift_swap'`, rispettando la sicurezza (T-SEC-08). | aperto |
