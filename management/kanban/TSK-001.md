---
id: TSK-001
title: "Next.js + Drizzle + shadcn/ui Scaffolding"
layer: be+fe
consumer: agent
estimate: S
status: done
wave: 1
depends_on: []
code_path: code/app/
---

# TSK-001 — Scaffolding monorepo

## Obiettivo
Inizializzare la struttura del progetto Next.js 15 App Router con tutte le dipendenze
dello stack scelto, pronta per lo sviluppo dei layer successivi.

## Scope

### 1. Next.js 15 + TypeScript
```bash
npx create-next-app@latest . --typescript --tailwind --app --src-dir=false \
  --import-alias "@/*" --eslint
```

### 2. Dipendenze principali
```json
{
  "dependencies": {
    "next": "^15.0.0",
    "@auth/drizzle-adapter": "latest",
    "next-auth": "^5.0.0",
    "drizzle-orm": "latest",
    "postgres": "latest",
    "@tanstack/react-table": "^8.0.0",
    "@tanstack/react-query": "^5.0.0",
    "zustand": "^5.0.0",
    "react-hook-form": "latest",
    "zod": "latest",
    "@hookform/resolvers": "latest",
    "date-fns": "^3.0.0",
    "@date-fns/tz": "latest",
    "lucide-react": "latest",
    "bcryptjs": "latest"
  },
  "devDependencies": {
    "drizzle-kit": "latest",
    "vitest": "latest",
    "@vitejs/plugin-react": "latest",
    "playwright": "latest",
    "@types/bcryptjs": "latest"
  }
}
```

### 3. shadcn/ui setup
```bash
npx shadcn@latest init
# Aggiungi: button, input, select, table, card, badge, dialog, dropdown-menu,
#           tabs, toast, skeleton, avatar, calendar, popover, separator
```

### 4. Struttura directory
```
code/app/
├── app/
│   ├── (auth)/login/page.tsx
│   ├── (admin)/layout.tsx       ← verifica ruolo admin
│   ├── (admin)/dashboard/page.tsx
│   ├── (employee)/layout.tsx    ← verifica ruolo employee
│   ├── (employee)/calendar/page.tsx
│   └── api/
├── components/ui/               ← shadcn/ui
├── components/matrix/           ← placeholder
├── components/requests/         ← placeholder
├── lib/rules/                   ← placeholder (TSK-006)
├── lib/zod/                     ← placeholder (TSK-007)
├── lib/auth/                    ← placeholder (TSK-003)
├── lib/date/                    ← date-fns/tz helpers
├── db/schema.ts                 ← placeholder (TSK-002)
├── db/migrations/
└── types/index.ts
```

### 5. Configurazioni
- `tailwind.config.ts` con palette Turnly (blue-500 primario, slate-50 bg)
- `tsconfig.json` strict mode, path aliases `@/*`
- `.env.example` con `DATABASE_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`
- `drizzle.config.ts` base

## Criteri di accettazione
- [ ] `npm run dev` avvia senza errori
- [ ] `npm run build` completa senza errori TypeScript
- [ ] `npm run lint` passa senza errori ESLint
- [ ] shadcn/ui `<Button>` importabile e renderizzabile
- [ ] `db/schema.ts` importabile senza errori (anche se vuoto per ora)
- [ ] `.env.example` documentato con tutte le variabili necessarie
