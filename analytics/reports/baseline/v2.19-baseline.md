---
type: baseline-metrics
version: v2.19
dataset_window: "<<es. 2026-06-15 → 2026-06-30>>"
events_count: <<N eventi reali in analytics/events/2026-06.jsonl>>
runs_consumed: 3
generated_at: "<<ISO-8601 UTC>>"
confidence: low   # N=3 run → confidence low; cresce con N (ADR-025 §A reference)
generator: /analytics report --baseline --version=v2.19
---

# Baseline metrics v2.19 — primo dataset reale del framework (dogfooding EP-013)

> **Template da compilare** dopo i 3 run di EP-012 (ADR-037). I placeholder `<<...>>`
> indicano i valori da misurare. Generabile on-demand via
> `/analytics report --baseline --version=v2.19` (Step 6 della skill
> `cost-and-time-analytics`). Consumer naturali: EP-014 (temporal budget governor) e
> EP-010 (estimation reference class).
>
> [^src: design_&_architecture/decisions/ADR-041.md §C §D §E]

## §1 Distribuzioni per layer

Dati prodotti da `analyze_timeline` (`.claude/tools/analytics/analyze-timeline.sh`).
P50/P85/P95 di `elapsed_ms` per layer:

| Layer | P50 (ms) | P85 (ms) | P95 (ms) | N eventi |
|-------|----------|----------|----------|---------|
| raw (ingest) | << >> | << >> | << >> | << >> |
| wiki (planning) | << >> | << >> | << >> | << >> |
| plan (design) | << >> | << >> | << >> | << >> |
| develop | << >> | << >> | << >> | << >> |
| review | << >> | << >> | << >> | << >> |

*Input per EP-014 (temporal budget governor): P85 per layer = reference class.*

## §2 Cost per capability

Token consumption per capability opt-in attivata:

| Capability | Tokens input | Tokens output | Costo stimato ($) |
|---|---|---|---|
| analytics (cabling EP-013) | << >> | << >> | << >> |
| premortem (se attivo) | << >> | << >> | << >> |
| compression.output (se attivo) | << >> | << >> | << >> |

## §3 Wave parallelism efficacy

Ratio di speedup parallel vs serial calcolato dagli eventi `wave_started`/`wave_completed`:

- Wave totali: << >>
- Speedup medio: << >>x (wall-clock parallel / stima serial)
- Wave più lunga: << >> ms (wave_id: << >>)
- Wave più corta: << >> ms (wave_id: << >>)

## §4 Capability non esercitate

Capability opt-in per cui il dataset NON ha eventi (sovrapposta con §6 dei RUN-REPORT di EP-012):

- visual-oracle: <<esercitata: Y/N>>
- a11y: <<esercitata: Y/N>>
- ux_ui: <<esercitata: Y/N>>
- compression.context: <<esercitata: Y/N>>

## §5 Anomalie

Outlier identificati (segnali early per T2 cecità temporale):

- `elapsed_ms > P95*2`: << eventi outlier >>
- `tokens_used > forecast*4`: << eventi outlier >>

## §6 Confidence

N=3 run → confidence: **low** (ADR-025 §A: N < 10 = confidence low).
Condizioni per upgrade: N ≥ 10 run → confidence medium; N ≥ 30 → high.

## §7 Self-observation

Telemetria meta-osservazione: il fatto che EP-013 abbia funzionato (eventi reali presenti):

- `events_recorded_during_v2.19_dev`: << N >> (proxy maturità cabling)
- Prima data di evento reale: << >>
- Ultima data di evento reale: << >>
